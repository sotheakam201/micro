spring:
  profiles:
    active: dev
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://${HOST}:9090/realms/demo
          jwk-set-uri: http://${HOST}:9090/realms/demo/protocol/openid-connect/certs
  application:
    name: product-service
